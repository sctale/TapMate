# 更新日志

## [0.5.0] - 2026-09-12

性能架构调整 + 厂商瘦身。

- **官网通道改「全屏对话页」**（参考 Msty Navigator / Cherry Studio 快应用）：不再嵌入首页内容区，点模型即整页打开官网聊天；顶部控制条（‹ 网页后退 / ⟳ 刷新 / ✕ 返回首页）为布局兄弟节点而非悬浮层 → **WebView 恢复硬件加速**（v0.4.1 为让悬浮球盖住网页被迫 software 层，是整个 App 卡顿的元凶），官网页内悬浮球自动隐藏
- **历史会话去 Modal**：改页内覆盖层（Android Modal 窗口 + fade 合成开销大），配合既有虚拟化/去 Svg/延帧加载，打开与滚动显著改善；返回键关闭
- **厂商精简为 4 家**：移除豆包（保留 ChatGPT / Gemini / DeepSeek / 通义千问），豆包官方图标资源一并删除
- 文案全站对齐「全屏对话页」心智（空态、菜单项「打开官网对话（全屏）」、配置页、引导卡、README）

## [0.4.3] - 2026-09-12

全面排查后的集中修复（悬浮球交互 / 历史面板 / 过时文案 / 打磨项）。

- **A 悬浮球交互**：菜单里选厂商/切子模型不再自动关闭（active 环即时反馈，点遮罩才关，连续切换不打断）；子模型横滑补 nestedScrollEnabled 修安卓手势被外层抢；**修 v0.4.0 起拖拽实际失效**（PanResponder 闭包捕获旧几何）改经 ref 读最新值 + 补 Terminate 落位；球定位改按"状态栏+键盘可用区"计算（不压通知栏、键盘弹出不再跳位）；球菜单贴球下方、球过低时自动上翻；首次"收进悬浮球"提示持久化只弹一次
- **B 历史面板**：有会话时固定 72% 高面板 + FlatList flex:1，修 FlatList 在 maxHeight-only 容器里塌缩（空白/不可滚回归）
- **C 文案**：清理多轮改版后残留的"模型胶囊/配置页"旧表述（空态、嵌入提示条、浏览器引导卡、对比退出提示、通道说明、登录确认按钮改「✅ 我已登录 · 回首页开聊」）
- **D 打磨**：错误气泡内联「🔁 重试」按钮（不用长按）；saveAndTest 拉模型清单纳入 busy 窗口防重复提交；BounceDot 动画值移入 ref；web 嵌入态 toast 下移；对比会话标签改 state 驱动修首帧缺失

## [0.4.2] - 2026-09-12

真机回归三项修复。

- **球与菜单点击无响应修复**：v0.4.1 遮罩 z-index(39) 盖过菜单/球吞掉所有点击——遮罩/菜单/球层级重排为 40/41/41
- **豆包图标**：改用官网官方 128px 图标位图（BrandGlyph 支持 矢量 path → 官方 PNG → emoji 三级回落），不再显示通用豆 emoji
- **历史会话卡顿修复**：列表行不再渲染品牌 Svg（改轻量 emoji）；会话列表改 FlatList 虚拟化；从球菜单点「🕘 历史」延后一帧开弹窗，与菜单关闭不再挤同一帧

## [0.4.1] - 2026-09-12

真机反馈四项修复。

- **切换模型失效修复**：球菜单整体改为可滚动容器（v0.4.0 菜单超一屏时底部厂商/动作条目点不到），行高加大到 54 更易命中
- **真实品牌图标**：悬浮球球面、展开菜单、配置页厂商卡、历史会话行统一改用品牌矢量图标（react-native-svg 渲染官方标志 path：OpenAI/Wikimedia、Gemini/DeepSeek/千问/simple-icons；豆包暂无可靠矢量源，保留"豆"emoji），不再自创图标
- **菜单统一球风格**：展开不再是文字列表，改为一纵列「球 + 左侧名称标签」，当前厂商球描边高亮，多模型厂商 ▾ 展开横滑模型胶囊
- **历史会话卡顿修复**：打开弹窗的查库推迟到淡入动画结束（InteractionManager）；消息列表虚拟化参数（分批 8/windowSize 9/removeClippedSubviews）；MessageBubble 与 MarkdownText memo 化、解析结果缓存——长会话打开与滚动不再整列重渲染

## [0.4.0] - 2026-09-12

交互重构：边缘手势与手机系统手势（全面屏 Home 上滑 / 顶部通知栏下拉）冲突，改用悬浮球统一承载。

- **悬浮球（TapBall）**：移除顶部 chrome 与底部 dock，一颗贴右悬浮球承载全部导航——默认显示当前模型（厂商 emoji，对比态 ⚖️、官网态 🌐 角标）；竖向可拖、松手吸附右缘、位置持久化；点按向下展开菜单，半透明遮罩点外收起（不再用定时自动收回）
- **展开菜单**：头行「厂商 · 当前模型」+ 厂商球组（多模型厂商点选后横向滑出具体模型小胶囊）+ 动作行（⚖️ 并发对比 / ✚ 新对话 / 🕘 历史 / ⚙️ 配置厂商与 Key）；官网嵌入态追加 ‹ 网页后退 / ⟳ 刷新 / ✕ 退出嵌入
- **配置页**：从球菜单进入，顶部「‹ 返回对话」+ Android 返回键回到对话；TabBar / ModelSwitcher 组件移除
- 嵌入官网 WebView 改 software 图层，保证悬浮球浮于网页之上；`clampBallRatio` 纯函数 + 单测
- 净减约 320 行：边缘手势条、peek 定时器、immersive 状态机、dock 隐藏逻辑全部移除

## [0.3.0] - 2026-09-11

竞品调研驱动的三大能力（借鉴 ChatALL 并发对比 / TypingMind・LobeChat 动态模型 / Cherry Studio・OpenCat 思考过程展示）。

- **💭 思考过程展示**：推理模型（deepseek-reasoner、GLM thinking、Kimi 等 `reasoning_content` 字段；Gemini thought parts）的思考内容流式呈现——还没有正文时自动展开"在想什么"，出答案后收起为「💭 思考过程 ▾」可随时点开；随消息落库，历史会话可回看
- **📥 动态模型清单**：API Key 保存成功即拉取账号真实模型列表（去噪过滤 whisper/tts/嵌入/图像类、去重、新模型优先、上限 50），替代过时的内置默认清单；配置页可随时「重新同步 / 恢复默认」；老库自动迁移新增存储表
- **⚖️ 并发对比模式**：顶栏新增 ⚖️，选 2-4 个 API 模型后一个问题同时发出，回答并排回流，每条带「厂商·模型」标签；各流独立缓冲/空闲超时/失败态，「■ 停止」一键全停；长按任意回答可「重试这条」（只重跑该模型，其它结果保留）；对比会话完整持久化，从历史打开自动恢复所选模型
- 流式引擎重构为多流注册表（每流闭包私有 buffer/节流/超时/幂等收尾），单模型即 N=1 特例；旧消息数据完全兼容（思考/标签字段缺省即隐藏）
- **厂商阵容调整**：移除 Kimi、智谱清言，新增**通义千问**（DashScope OpenAI 兼容端点 + 通义官网登录），内置厂商 6 → 5 家
- 新增单测：模型清单解析/过滤 7 项（累计 16 项）

## [0.2.3] - 2026-09-11

首页空间让位给对话：图标栏瘦身 + 嵌入控件去常驻 + 全面沉浸手势。

- **圆形模型栏**：顶部一排大胶囊改为圆形厂商图标（40px，选中描边环）；多模型厂商带数字角标，选中后下方展开一行小胶囊选具体模型；官网通道带 🌐 角标。标题与 ✚/🕘 按钮同步瘦身
- **嵌入对话去顶栏**：常驻的「嵌入官网对话」标题条移除，控件收进网页右上角小浮条（‹ 后退 / ⟳ 刷新 / ✕ 退出），数秒自动淡出，点屏幕顶边随时唤出
- **沉浸模式**：点选任一模型（或打开历史/登录确认）后，顶部 chrome 与底部 dock 全部收起，聊天区全屏；点顶边/下滑手势浮出模型栏 3.6 秒自动收回；从屏幕最底边上滑唤出导航 dock、下滑收回；进入沉浸时一次性手势教学 toast
- 配置页始终保留底部导航；Android 返回键在嵌入态仍先网页后退、再退出嵌入
- **品牌统一**：取消中文名副名（「一点搭子」→ 弃用，短暂尝试的「百应」亦作废），应用内外统一只叫 **TapMate**；桌面显示名、首页标题、首启引导卡、README 全部同步

## [0.2.2] - 2026-09-10

### 体验

- **品牌命名落地**：确定中文名「一点搭子」，App 内顶部标题与首启引导卡由「TapMate」更新为「一点搭子」（呼应 TapMate 的 mate 本义，聚合多模型任选一位"搭子"）

## [0.2.1] - 2026-09-10

产品定位修正：无论用哪种方式连接 AI，连接完成后对话都应在首页完成，不再跳转第二个页面。

### 官网通道重构为「首页嵌入式对话」（参考 Msty 等聚合客户端的外壳模式）

- 选择官网通道模型后，官网聊天界面（含其输入框）**直接嵌入首页内容区**，在首页输入、在首页看回复，全程零跳转；顶栏提供 ⟳ 刷新 / ‹ 后退 / ✕ 退出，退出后可点模型胶囊或引导按钮随时重开
- 修复「点击『✅ 我已登录，开始使用』后无法使用」：确认登录后自动关闭容器、切回首页、选中该厂商并立即进入嵌入对话；登录容器从此只承担首次登录职责
- 废弃 v0.2.0 的过渡方案（"复制草稿去官网"）：官网模型下首页不再渲染原生输入区，改为引导条说明当前对话方式
- Gemini 等 customTabs 厂商：首页展示合规引导卡，说明 Google 政策原因并提供「在浏览器打开」一键入口（系统浏览器共享 Chrome 登录态）
- 登录确认不再强改用户所选通道；WebView UA/属性抽为共享配置（webConfig），登录容器与嵌入组件保持同源

### 文案与引导

- 配置页通道说明、首启引导卡同步为「嵌入首页对话」心智（官网通道 = 登录后在首页直接聊，免费用订阅额度）

## [0.2.0] - 2026-09-10

按全面体验走查（Nielsen 十原则 + 适配/性能，32 项 findings）的系统性修复与体验补齐。

### 对话核心闭环

- **停止生成**：流式期间发送按钮变「■ 停止」，可中断在途回答并保留已生成内容（此前 abort 函数从未接入 UI）
- **新对话入口**：顶部新增「✚」，一键中断在途流并开新会话；切换模型/打开历史不再被旧流锁住发送按钮
- **Markdown 渲染**：模型回复支持代码块、标题、列表、引用、加粗/斜体/行内代码/链接（零依赖轻量渲染器，未覆盖语法自动降级为文本）
- **消息长按动作**：复制内容、重新生成、错误回答一键重试
- **生成中指示**：assistant 空气泡显示跳动圆点「正在思考…」，不再是一片空白
- **智能滚动**：仅贴近底部时自动跟随流式更新；上翻回看不再被反复拽回底部，并出现「回到底部 ↓」悬浮按钮
- **记住上次模型**：冷启动恢复上次使用的模型（修复定义了 `LAST_MODEL` 却无人消费的遗留问题）
- Enter 键行为显式化为换行（`submitBehavior="newline"`），删除未使用的 `APP_EVENTS` 死常量

### 官网通道（web / customTabs）

- **不再丢失草稿**：官网通道点「打开官网」时，输入内容保留在输入框并自动复制到剪贴板（此前被静默清空）
- **Android 返回键**：官网容器内优先网页后退，无路可退才关闭覆盖层；顶部新增「‹」后退按钮
- **加载失败兜底**：WebView 打不开时显示错误卡片 + 重试 + 「用系统浏览器打开」降级入口（ChatGPT/Gemini 域名不可达不再无限白屏）
- **空态与占位文案**更新为符合官网通道真实行为（"点「打开官网」与 X 对话…"）

### 异常与数据安全

- **删除会话二次确认**（Alert 不可恢复提示），删除按钮 ✕ 改 🗑 警示色，与「关闭」的 ✕ 语义分离
- **流式空闲超时**：45 秒无增量自动按错误收尾，输入框不再被悬挂连接永久锁死
- **报错人话化**：401/403、429、5xx、网络错误、上下文超限分别映射为可行动文案；Key 测试失败区分超时/网络/鉴权/状态码
- **长会话上下文裁剪**：发送时按字符预算保留最近轮次，避免超出小上下文模型限制
- 登录态显式确认不变量（0.1.1 引入）保持不变

### 配置与上手

- 通道重命名为收益导向：「应用内官网 / 系统浏览器 / API Key」，选中即展开一句话差异说明
- 新增「解绑此厂商」（removeConfig 首次接入 UI），误点「我已登录」或换账号可清除
- API Key 支持 👁 明密文切换；新增可选「自定义 Base URL」输入（中转端点从半成品类型承诺变为可用）
- 「获取 API Key」改为可点击链接（Linking 打开厂商文档页）
- 首次启动双通道说明卡（一次性，看过不再弹）；轻反馈升级为全局 Toast（配置/对话页共用）

### 平台与适配

- 历史会话支持搜索（标题+内容 LIKE）与 今天/昨天/7 天内/更早 分组；小触控目标统一补 hitSlop
- Android 显式声明 `softwareKeyboardLayoutMode: resize`；配置页接入 KeyboardAvoidingView，toast 不再被键盘遮挡
- 模型切换器选中项自动滚入视野

### 依赖

- 新增 `expo-clipboard`（复制消息/草稿）

## [0.1.1] - 2026-09-10

### Bug 修复

- **登录态误判修复**：此前打开官网即自动标记"已连接"，未登录也会显示已连接。现改为显式确认——在官网完成登录后点击「✅ 我已登录」才记录，点 ✕ 关闭不标记
- **官网通道对话打通**：此前首页模型列表只包含 API 通道的模型，登录 DeepSeek 等官网后首页无法对话。现 web/customTabs 通道厂商也会出现在首页（🌐 标记），输入消息后直接打开官网对话容器
- **更换 App 图标**：生成 Tap 系列风格图标（暖米白底 + 靛蓝三气泡），配置 adaptive icon

## [0.1.0] - 2026-09-10

首个版本：多模型聚合对话 App

### 新功能

- **双通道接入**：网页登录（应用内 WebView）/ 浏览器登录（Chrome Custom Tabs，共享 Chrome 登录态）/ API Key 三种接入方式，优先官方通道、减少 API 费用
- **内置 6 家厂商**：ChatGPT、Gemini、DeepSeek、Kimi、智谱清言、豆包；国内厂商统一走 OpenAI 兼容协议
- **统一对话页**：模型切换器（厂商色点胶囊）、SSE 流式渲染（120ms 节流）、消息气泡（用户靛蓝 / 模型白卡）
- **配置页**：厂商卡片列表、通道切换、API Key 保存并自动测试连通性、官网登录入口
- **会话管理**：本地持久化（expo-sqlite）、首条消息自动命名、历史会话列表与删除
- **安全**：API Key 加密存储（expo-secure-store / Android Keystore），纯客户端无后端

### 体验

- UI 延续 Tap 系列治愈暖色风格（暖米白背景 + 靛蓝强调色 + 悬浮胶囊导航，对齐 TapLedger）
