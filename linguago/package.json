{
  "name": "linguago",
  "version": "0.1.0",
  "private": true,
  "description": "LinguaGo 多语学堂 —— 英/日/韩多语种沉浸式在线语言学习平台",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install",
    "dev": "npm --prefix server run dev & npm --prefix web run dev",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix server run typecheck && npm --prefix web run build",
    "test": "npm --prefix server run test"
  }
}
